```
flowchart TD
    Start([execute]) --> CheckNull{command<br/>== null?}
    CheckNull -->|是| ThrowNPE[抛NullPointerException]
    CheckNull -->|否| GetCtl[获取ctl]
    
    GetCtl --> CheckCore{workerCount<br/>< corePoolSize?}
    CheckCore -->|是| AddCore[addWorker<br/>command, true]
    AddCore -->|成功| Return1[返回]
    AddCore -->|失败| GetCtl2[重新读取ctl]
    
    CheckCore -->|否| CheckRunning{isRunning?}
    GetCtl2 --> CheckRunning
    
    CheckRunning -->|否| Reject[reject<br/>拒绝任务]
    CheckRunning -->|是| Offer[workQueue.offer]
    
    Offer -->|失败| AddMax[addWorker<br/>command, false]
    AddMax -->|成功| Return2[返回]
    AddMax -->|失败| Reject
    
    Offer -->|成功| Recheck[重新检查状态]
    Recheck -->|Shutdown| Remove[remove<br/>从队列移除] --> Reject
    Recheck -->|Running| CheckZero{workerCount<br/>== 0?}
    CheckZero -->|是| AddEmpty[addWorker<br/>null, false]
    CheckZero -->|否| Return3[返回]
    
    AddCore -.-> Worker[Worker线程<br/>runWorker]
    AddMax -.-> Worker
    AddEmpty -.-> Worker
    
    subgraph WorkerLoop [Worker执行循环]
        Worker --> Unlock[unlock<br/>允许中断]
        Unlock --> Loop{while<br/>task!=null<br/>或getTask!=null}
        Loop -->|是| Lock[lock<br/>防中断]
        Lock --> Before[beforeExecute]
        Before --> Run[task.run]
        Run --> After[afterExecute]
        After --> Unlock2[unlock]
        Unlock2 --> GetTask[getTask]
        GetTask --> Loop
        Loop -->|否| Exit[processWorkerExit]
    end
    
    subgraph GetTaskLogic [getTask逻辑]
        GetTask --> CheckShut{Shutdown<br/>且Stop或队空?}
        CheckShut -->|是| Dec[workerCount--] --> RetNull[return null]
        CheckShut -->|否| CheckTime{allowTimeout<br/>或wc>core?}
        CheckTime -->|是| Poll[poll限时] --> Got{获得任务?}
        CheckTime -->|否| Take[take阻塞] --> Got
        Got -->|是| RetTask[return task]
        Got -->|否| CheckT{timedOut<br/>且可退出?}
        CheckT -->|是| Dec2[workerCount--] --> RetNull
        CheckT -->|否| Retry[重试]
        Retry --> GetTask
    end
    
    RetNull -.-> Exit
    
    classDef start fill:#e1f5e1,stroke:#2e7d32
    classDef error fill:#ffebee,stroke:#c62828
    classDef loop fill:#f3e5f5,stroke:#6a1b9a
    classDef decision fill:#fff3cd,stroke:#f9a825
    
    class Start,Return1,Return2,Return3 start
    class ThrowNPE,Reject,RetNull error
    class Worker,WorkerLoop,GetTaskLogic loop
    class CheckNull,CheckCore,CheckRunning,CheckZero,Loop,CheckShut,CheckTime,Got,CheckT decision
```

提交任务
    ↓
1. 核心线程数检查（corePoolSize）
   └─ 不足：创建核心线程（addWorker(command, true)）
   ↓
2. 任务入队（workQueue.offer）
   └─ 成功：双检查（Double Check）机制
   ↓
3. 最大线程数检查（maximumPoolSize）
   └─ 不足：创建临时线程（addWorker(command, false)）
   ↓
4. 拒绝策略（Reject）